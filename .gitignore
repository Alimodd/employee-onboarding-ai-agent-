# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Local persistent Chroma vector database
chroma_db/

# Local SQLite ticket database + evaluation output
data/tickets.db
evaluation/results.json

# Company policy documents (kept local only, not published)
company_docs/

# Planning / course documents (kept local only, not published)
*.docx

# Virtual environments
.venv/
venv/
env/
